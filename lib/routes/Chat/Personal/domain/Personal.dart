class Personal {
  // Personal(this.id, this.name, this.date, this.text, this.uid);
  Personal(this.uid, this.name, this.documentId);
  String uid;
  String name;
  String documentId;
  // String date;
  // String text;
  // String uid;
}